// BUILT IN TYPES(attribute or method);
// objectives
// Numbers(int, double);
// Strings(string);
// Booleans(bool);
// List(List, also Known as arrays);
// Sets(set);
// Maps(Map);
// The value null (Null);
// Runes (Runes: often replace by the characters API);
// Symbols (Symbol);

void main() {
  var myName = 'Usman';
  // this is an example of an attribute
  // print(myName.length);
  print(myName.toUpperCase());
  myName = myName.toUpperCase();
  print(myName);
}
