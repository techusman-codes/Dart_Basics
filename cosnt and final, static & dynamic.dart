void main() {
  // final
  // var speed = 1000;
  // final version = speed * 2;
  // print(version);

  // cosnt
  const version = 2.1;
  print(version);

  // what is the difference between const & final
  // final can be reassign in future(you can use it and change it in the future)
  // const is more restricted (it cant be change later on)

  // STATIC AND DYNAMIC

  // STATIC TYPING the values must maintain same Data types
  // if you decleared a data type as string you cant assign it back to int
  // var a = 100; int
  // a = 'hundred' //string;

  // DYNAMIC TYPING
  // this can be assign and overide by any data type if you use 'dynamic keyword at the beginnin'
  // DO NOT USE DYNAMIC TYPING UNLESS YOU HAVE IT
}
